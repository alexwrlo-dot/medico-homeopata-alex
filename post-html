<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Artículo - Dr. Alex Reyes</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Nunito:wght@400;600;700;800&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Outlined" rel="stylesheet">
    
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: "#7c9be3",
                        secondary: "#b599d9",
                        accent: "#6b9aeb",
                        neubg: "#e0e5ec",
                        neutxt: "#4a5568",
                        neutxtlight: "#718096",
                    },
                    fontFamily: {
                        display: ["Nunito", "sans-serif"],
                        body: ["Inter", "sans-serif"],
                    },
                },
            },
        };
    </script>

    <style>
        * {
            scroll-behavior: smooth;
            box-sizing: border-box;
        }

        body {
            background: #e0e5ec;
            color: #4a5568;
            min-height: 100vh;
            font-family: 'Inter', sans-serif;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: 
                url('https://images.unsplash.com/photo-1576091160399-112ba8d25d1d?w=1200&auto=format&fit=crop'),
                linear-gradient(135deg, #e0e5ec 0%, #f0f4f8 100%);
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            opacity: 0.08;
            z-index: -1;
            pointer-events: none;
        }

        .neu-panel {
            background: #e0e5ec;
            border-radius: 24px;
            box-shadow: 9px 9px 16px rgba(163, 177, 198, 0.6), -9px -9px 16px rgba(255, 255, 255, 0.5);
        }

        .neu-card {
            background: #e0e5ec;
            border-radius: 20px;
            box-shadow: 5px 5px 10px rgba(163, 177, 198, 0.6), -5px -5px 10px rgba(255, 255, 255, 0.5);
        }

        .back-button {
            position: fixed;
            top: 2rem;
            left: 2rem;
            z-index: 1000;
            background: linear-gradient(145deg, #6b9aeb, #7c9be3);
            color: white;
            width: 56px;
            height: 56px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 6px 6px 12px rgba(163, 177, 198, 0.6), -6px -6px 12px rgba(255, 255, 255, 0.5);
            cursor: pointer;
            transition: all 0.3s ease;
            border: none;
        }

        .back-button:hover {
            box-shadow: 10px 10px 20px rgba(163, 177, 198, 0.7), -10px -10px 20px rgba(255, 255, 255, 0.6);
            transform: translateX(-5px) scale(1.05);
        }

        .loading {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(124, 155, 227, 0.3);
            border-radius: 50%;
            border-top-color: #7c9be3;
            animation: spin 1s ease-in-out infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        /* Estilos para contenido Markdown */
        .post-content h1, .post-content h2, .post-content h3 {
            color: #4a5568;
            font-weight: 700;
            margin: 1.5em 0 0.5em;
        }

        .post-content h1 { font-size: 2em; }
        .post-content h2 { font-size: 1.5em; }
        .post-content h3 { font-size: 1.25em; }

        .post-content p {
            margin: 1em 0;
            line-height: 1.8;
            color: #4a5568;
        }

        .post-content ul, .post-content ol {
            margin: 1em 0;
            padding-left: 2em;
            color: #4a5568;
        }

        .post-content li {
            margin: 0.5em 0;
            line-height: 1.6;
        }

        .post-content strong {
            color: #7c9be3;
            font-weight: 600;
        }

        .post-content a {
            color: #6b9aeb;
            text-decoration: underline;
        }

        .post-content img {
            max-width: 100%;
            border-radius: 15px;
            margin: 1.5em 0;
            box-shadow: 5px 5px 15px rgba(163, 177, 198, 0.5);
        }

        .post-content blockquote {
            border-left: 4px solid #7c9be3;
            padding-left: 1.5em;
            margin: 1.5em 0;
            font-style: italic;
            color: #718096;
        }

        .post-content code {
            background: rgba(124, 155, 227, 0.1);
            padding: 0.2em 0.4em;
            border-radius: 4px;
            font-family: monospace;
            color: #7c9be3;
        }

        .post-content pre {
            background: rgba(124, 155, 227, 0.1);
            padding: 1em;
            border-radius: 8px;
            overflow-x: auto;
            margin: 1em 0;
        }

        @media (max-width: 768px) {
            .back-button {
                top: 1rem;
                left: 1rem;
                width: 48px;
                height: 48px;
            }
        }
    </style>
</head>
<body>

    <!-- Botón de retorno -->
    <button class="back-button" onclick="window.close()" title="Volver">
        <span class="material-icons-outlined text-3xl">arrow_back</span>
    </button>

    <div class="container mx-auto px-4 sm:px-6 py-12 sm:py-16">
        
        <!-- Loading -->
        <div id="post-loading" class="text-center mt-20">
            <div class="loading mx-auto"></div>
            <p class="text-neutxtlight mt-4">Cargando artículo...</p>
        </div>

        <!-- Post Content -->
        <article id="post-article" class="hidden max-w-4xl mx-auto">
            
            <!-- Header del Post -->
            <div class="neu-panel p-8 sm:p-12 rounded-3xl mb-8">
                <div id="post-image-container" class="hidden mb-6">
                    <img id="post-image" src="" alt="" class="w-full h-64 sm:h-96 object-cover rounded-2xl">
                </div>
                
                <h1 id="post-title" class="text-3xl sm:text-4xl lg:text-5xl font-bold font-display mb-4 text-neutxt"></h1>
                
                <div class="flex items-center gap-4 text-sm text-neutxtlight flex-wrap">
                    <div class="flex items-center gap-2">
                        <span class="material-icons-outlined text-accent">calendar_today</span>
                        <span id="post-date"></span>
                    </div>
                    <div id="post-author-container" class="flex items-center gap-2 hidden">
                        <span class="material-icons-outlined text-accent">person</span>
                        <span id="post-author"></span>
                    </div>
                </div>
            </div>

            <!-- Contenido del Post -->
            <div class="neu-card p-8 sm:p-12 rounded-3xl">
                <div id="post-content" class="post-content text-base sm:text-lg">
                    <!-- Contenido Markdown se renderiza aquí -->
                </div>
            </div>

            <!-- Botón de Retorno al Final -->
            <div class="text-center mt-8">
                <button onclick="window.close()" class="neu-card px-8 py-4 rounded-full font-semibold hover:shadow-lg transition inline-flex items-center gap-2">
                    <span class="material-icons-outlined">arrow_back</span>
                    Volver a los artículos
                </button>
            </div>

        </article>

        <!-- Error Message -->
        <div id="post-error" class="hidden text-center mt-20">
            <div class="neu-panel p-12 rounded-3xl max-w-md mx-auto">
                <span class="material-icons-outlined text-8xl text-neutxtlight mb-6">error_outline</span>
                <h2 class="text-2xl font-bold text-neutxt mb-4">No se pudo cargar el artículo</h2>
                <p class="text-neutxtlight mb-6">El artículo solicitado no existe o no está disponible.</p>
                <button onclick="window.close()" class="neu-card px-8 py-4 rounded-full font-semibold hover:shadow-lg transition">
                    Volver
                </button>
            </div>
        </div>

    </div>

    <script>
        // 🔧 CONFIGURAR: Tus credenciales de GitHub
        const USER = "alexwrlo-dot";
        const REPO = "medico-homeopata-alex";

        // Obtener el nombre del archivo del post desde la URL
        const urlParams = new URLSearchParams(window.location.search);
        const postFile = urlParams.get('file');

        async function cargarPost() {
            const loading = document.getElementById('post-loading');
            const article = document.getElementById('post-article');
            const error = document.getElementById('post-error');

            if (!postFile) {
                loading.classList.add('hidden');
                error.classList.remove('hidden');
                return;
            }

            try {
                // Obtener contenido del archivo
                const response = await fetch(`https://api.github.com/repos/${USER}/${REPO}/contents/blog/${postFile}`);
                
                if (!response.ok) {
                    throw new Error('Post no encontrado');
                }

                const fileData = await response.json();
                const contentResponse = await fetch(fileData.download_url);
                const content = await contentResponse.text();

                // Parsear frontmatter
                const frontmatterRegex = /^---\s*\n([\s\S]*?)\n---/;
                const match = content.match(frontmatterRegex);
                
                let frontmatter = {};
                let postContent = content;

                if (match) {
                    match[1].split('\n').forEach(line => {
                        const [key, ...valueParts] = line.split(':');
                        if (key && valueParts.length) {
                            frontmatter[key.trim()] = valueParts.join(':').trim().replace(/['"]/g, '');
                        }
                    });
                    postContent = content.replace(frontmatterRegex, '').trim();
                }

                // Actualizar título de la página
                document.title = `${frontmatter.title || 'Artículo'} - Dr. Alex Reyes`;

                // Mostrar imagen si existe
                if (frontmatter.image) {
                    document.getElementById('post-image').src = frontmatter.image;
                    document.getElementById('post-image').alt = frontmatter.title || 'Imagen del artículo';
                    document.getElementById('post-image-container').classList.remove('hidden');
                }

                // Mostrar título
                document.getElementById('post-title').textContent = frontmatter.title || 'Sin título';

                // Mostrar fecha
                if (frontmatter.date) {
                    document.getElementById('post-date').textContent = new Date(frontmatter.date).toLocaleDateString('es-ES', {
                        year: 'numeric',
                        month: 'long',
                        day: 'numeric'
                    });
                }

                // Mostrar autor
                if (frontmatter.author) {
                    document.getElementById('post-author').textContent = frontmatter.author;
                    document.getElementById('post-author-container').classList.remove('hidden');
                }

                // Renderizar contenido Markdown
                document.getElementById('post-content').innerHTML = marked.parse(postContent);

                // Mostrar artículo
                loading.classList.add('hidden');
                article.classList.remove('hidden');

            } catch (err) {
                console.error('Error cargando post:', err);
                loading.classList.add('hidden');
                error.classList.remove('hidden');
            }
        }

        document.addEventListener('DOMContentLoaded', cargarPost);
    </script>

</body>
</html>
